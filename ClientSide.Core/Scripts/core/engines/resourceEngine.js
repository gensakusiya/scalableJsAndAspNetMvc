(function (core) {

    'use strict';

    var resourceEngine = function(description) {



        return {
            
        };

    }(core.Descriptions.Resource);

    core.addPartNamespace('Engines', 'ResourceEngine', resourceEngine);

})(ApplicationCore);