(function (core) {

    var description = [
        {
            Path: '/transfersList',
            Sandbox: 'TransfersList'
        }
    ];
    
    core.addPartNamespace('Descriptions', 'Routing', description);

})(ApplicationCore);